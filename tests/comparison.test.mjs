import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('scalable city discovery and bounded comparison are present',()=>{const cities=read('cities/index.html'),comparison=read('comparison/index.html'),home=read('index.html');for(const route of ['/new-york','/milan','/paris','/shanghai','/copenhagen','/tokyo'])assert.match(cities,new RegExp(`href="${route}"`));assert.match(home,/href="\/cities">Seven cities/);for(const word of ['relay','clock','inheritance','legitimacy','synchronization','verification','translation'])assert.match(comparison,new RegExp(word,'i'));assert.match(comparison,/do not establish that one city is more advanced/);assert.match(read('sitemap.xml'),/<loc>https:\/\/tech-from-here-fashion-week.vercel.app\/comparison<\/loc>/)});
